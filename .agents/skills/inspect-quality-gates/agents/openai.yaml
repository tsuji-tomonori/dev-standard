interface:
  display_name: "品質ゲート検査"
  short_description: "Inspect only selected checks and current evidence"
  default_prompt: "Use $inspect-quality-gates to run only change-relevant checks, return an explicit no-op when none apply, use the optional runner only with a target-owned registry, and report declared effects, authority, and process-effect isolation uncertainty for every command without changing repository policy."
