interface:
  display_name: "工程文書作成"
  short_description: "Create lifecycle evidence required by a concrete duty"
  default_prompt: "Use $author-lifecycle-docs only when a concrete legal, production, safety, contractual, or audit retention duty requires it, and create only the minimum evidence and traceability for that duty."
