interface:
  display_name: "適正規模実行"
  short_description: "最小十分な実行範囲を推定し、根拠付きで段階拡張する"
  default_prompt: "Use $right-size-execution to estimate the smallest sufficient execution scope, expand only on evidence, stop after decisive success, and preserve the efficiency ledger."
