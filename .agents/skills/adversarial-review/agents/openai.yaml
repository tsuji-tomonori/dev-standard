interface:
  display_name: "敵対的検証"
  short_description: "Assume mistakes may exist and challenge claims with independent evidence"
  default_prompt: "Use $adversarial-review to review without mutating the artifact and return either reproducible findings with conditional repair handoffs or an explicit scoped no-finding result with residual uncertainty."
