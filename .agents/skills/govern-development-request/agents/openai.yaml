interface:
  display_name: "統制付き開発要求"
  short_description: "Authorize once then execute every lifecycle quality gate"
  default_prompt: "Use $govern-development-request to define one complete execution boundary, obtain initial authorization, and finish autonomously."
