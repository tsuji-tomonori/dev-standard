interface:
  display_name: "統制付き開発要求"
  short_description: "Run the regulated work-item and gate lifecycle"
  default_prompt: "Use $govern-development-request only for a regulated change, bind its explicit authorization once, and execute the selected lifecycle gates within that boundary."
