interface:
  display_name: "フロントエンド体験テスト"
  short_description: "変更riskに該当するfrontend次元だけを検証する"
  default_prompt: "Use $test-frontend-experience to select only applicable frontend test dimensions, verify them against canonical requirements and approved design, and bind any declared external command effect to authority without claiming process isolation."
