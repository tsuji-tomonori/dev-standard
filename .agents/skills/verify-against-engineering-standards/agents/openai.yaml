interface:
  display_name: "標準・ベストプラクティス検証"
  short_description: "版と鮮度を固定したSWEBOK・cloud公式checklistで成果物を検証する"
  default_prompt: "Use $verify-against-engineering-standards to select only applicable controls from the Skill's default registry, verify freshness, and report evidence-bounded findings; generate repository-confined source docs only when requested."
