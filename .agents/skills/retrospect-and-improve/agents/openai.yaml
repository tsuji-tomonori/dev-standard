interface:
  display_name: "振り返りと改善"
  short_description: "Turn evidenced process failure into one improvement"
  default_prompt: "Use $retrospect-and-improve only for direct evidence of an escaped defect, incident, rollback, or recurring cost; propose one bounded, measurable change with a lighter alternative, rollback, and sunset without auto-applying it."
