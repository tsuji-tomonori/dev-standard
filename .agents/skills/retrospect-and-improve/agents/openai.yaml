interface:
  display_name: "振り返りと改善"
  short_description: "Turn evidenced process failure into one improvement"
  default_prompt: "Use $retrospect-and-improve only because a regulated close, escaped defect, repeated correction, critical miss, rollback, repeated CI repair, or periodic audit triggered it; propose one testable change without auto-applying it."
