interface:
  display_name: "権限境界の承認"
  short_description: "Bind an explicit decision to a real authority boundary"
  default_prompt: "Use $authorize-autonomous-execution to check the result and effects against current explicit authorization. Reuse valid approval; ask only for missing authority. Never mint or rewrite regulated authority evidence."
