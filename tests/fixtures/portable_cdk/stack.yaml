Parameters:
  Stage:
    Type: String
    Default: dev
Resources:
  Bucket:
    Type: AWS::S3::Bucket
    Metadata:
      RequirementIds: [REQ-BUCKET-001]
    Properties:
      BucketName: !Ref Stage
