# Merge these tables into the target repository's `.codex/config.toml`.
# Do not replace existing target-specific configuration.

[features]
hooks = true

[agents]
max_threads = 3
max_depth = 1
interrupt_message = true
